{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Go Run",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}", 
            "args": ["arg1", "arg2"]
        }
    ]
}
